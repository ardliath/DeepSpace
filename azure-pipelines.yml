--- 
pool: 
  vmImage: "Ubuntu 16.04"
steps: 
  - 
    script: |-
        dotnet build --configuration $(buildConfiguration)
        dotnet test dotnetcore-tests --configuration $(buildConfiguration)
variables: 
  buildConfiguration: Release
